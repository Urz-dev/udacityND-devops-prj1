{
  "type":"custom",
  "properties": {
    "displayName": "verify that a tag exists on a resource.Only indexed resources are checked so, Resource groups are not checked",
    "mode": "Indexed",
    
    "description": "Enforces a required tag and its value. Does not apply to resource groups.",
    "metadata": {
      "createdBy": "Umair.R"
    },
    "policyRule": {
      "if": {
          "field": "['tags']",
          "exists": "false"
      },
      "then": {
        "effect": "deny"
      }
    }
  }
}